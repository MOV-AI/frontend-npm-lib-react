import React from "react";
import { ConfirmModalProps } from "./types";
declare const ConfirmAlertModal: ({ onSubmit, onCancel, open, title, message, submitText, submitColor, cancelText, cancelColor, children }: ConfirmModalProps) => React.JSX.Element;
export default ConfirmAlertModal;
